# 🔍 ТЕСТИРОВАНИЕ API

## Проверка работы бэкенда:

### 1. Проверьте, что сервер запущен:
```bash
cd backend
npm run dev
```

Должно появиться:
```
✅ База данных инициализирована успешно
🚀 Сервер запущен на порту 3001
✅ Сервер готов к работе!
```

### 2. Проверьте работу API напрямую:
```bash
curl -X POST http://127.0.0.1:3001/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "full_name": "Test User",
    "email": "test@test.com",
    "login": "testuser",
    "password": "test123",
    "role": "admin"
  }'
```

Если это работает - бэкенд в порядке.

### 3. Проверьте работу через proxy:
Откройте браузер и перейдите на:
```
http://localhost:3000
```

В консоли браузера (F12) выполните:
```javascript
fetch('/api/auth/register', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    full_name: 'Test User',
    email: 'test@test.com',
    login: 'testuser',
    password: 'test123',
    role: 'admin'
  })
})
.then(r => r.json())
.then(console.log)
.catch(console.error)
```

### 4. Проверьте порты:
```bash
lsof -i:3000 -i:3001
```

Должны быть:
- Порт 3000: Vite (фронтенд)
- Порт 3001: Node.js (бэкенд)

## Если не работает:

1. **Убедитесь, что оба сервера запущены**
2. **Проверьте, что нет конфликтов портов**
3. **Перезапустите оба сервера**
4. **Очистите кеш браузера (Ctrl+Shift+R)**

