# 🔄 ПЕРЕЗАПУСК ВСЕГО ПРОЕКТА

## ✅ ЧТО БЫЛО ИСПРАВЛЕНО:

1. **Бэкенд теперь слушает на 0.0.0.0** (все интерфейсы)
2. **Vite proxy настроен на 127.0.0.1** вместо localhost
3. **Добавлены настройки secure: false и ws: true** для proxy

## 🚀 ШАГИ ДЛЯ ИСПРАВЛЕНИЯ:

### 1. ОСТАНОВИТЕ ВСЕ ПРОЦЕССЫ:

#### Остановите бэкенд:
```bash
cd backend
./stop-server.sh
```

#### Остановите фронтенд:
Нажмите `Ctrl+C` в терминале, где запущен фронтенд

### 2. ПРОВЕРЬТЕ, ЧТО ПОРТЫ СВОБОДНЫ:

```bash
lsof -i:3000 -i:3001
```

Если есть процессы - убейте их:
```bash
lsof -ti:3000 | xargs kill -9
lsof -ti:3001 | xargs kill -9
```

### 3. ЗАПУСТИТЕ БЭКЕНД:

```bash
cd backend
npm run dev
```

**Дождитесь сообщения:**
```
✅ База данных инициализирована успешно
🚀 Сервер запущен на порту 3001
✅ Сервер готов к работе!
```

### 4. ЗАПУСТИТЕ ФРОНТЕНД (В НОВОМ ТЕРМИНАЛЕ):

```bash
cd frontend
npm run dev
```

**Должно появиться:**
```
➜  Local:   http://localhost:3000/
```

### 5. ПРОВЕРЬТЕ РАБОТУ:

1. Откройте браузер: `http://localhost:3000`
2. Попробуйте зарегистрироваться
3. В консоли бэкенда должно появиться: `📥 POST /api/auth/register`

## 🔍 ЕСЛИ ВСЕ ЕЩЕ НЕ РАБОТАЕТ:

### Проверьте в консоли браузера (F12):

Выполните в консоли:
```javascript
fetch('/api/auth/register', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    full_name: 'Test',
    email: 'test@test.com',
    login: 'test',
    password: 'test123',
    role: 'admin'
  })
})
.then(r => r.json())
.then(console.log)
.catch(console.error)
```

Если это работает - проблема в коде компонента.
Если не работает - проблема в proxy или сервере.

## ⚠️ ВАЖНО:

1. **ОБА сервера должны быть запущены одновременно**
2. **Фронтенд на порту 3000, бэкенд на порту 3001**
3. **Перезапустите ОБА сервера после изменений**

## 🎯 БЫСТРАЯ ПРОВЕРКА:

```bash
# Проверка бэкенда
curl http://127.0.0.1:3001/api/auth/register -X POST -H "Content-Type: application/json" -d '{"full_name":"Test","email":"test@test.com","login":"test","password":"test123","role":"admin"}'

# Должен вернуть JSON с токеном
```

Если это работает - бэкенд в порядке, проблема в proxy или фронтенде.

---

**ВЫПОЛНИТЕ ВСЕ ШАГИ ПОСЛЕДОВАТЕЛЬНО!**

