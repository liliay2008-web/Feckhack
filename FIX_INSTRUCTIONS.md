# 🔧 ИНСТРУКЦИЯ ПО ИСПРАВЛЕНИЮ ОШИБКИ РЕГИСТРАЦИИ

## ✅ ЧТО БЫЛО ИСПРАВЛЕНО:

1. **Порядок инициализации сервера** - БД теперь инициализируется ДО регистрации маршрутов
2. **Улучшенная обработка ошибок** - детальное логирование всех ошибок
3. **Проверка готовности БД** - добавлена проверка в маршруте регистрации

## 🚀 КАК ПЕРЕЗАПУСТИТЬ СЕРВЕР:

### Вариант 1: Автоматический скрипт (РЕКОМЕНДУЕТСЯ)
```bash
cd backend
./restart.sh
```

### Вариант 2: Вручную
```bash
cd backend
./stop-server.sh
sleep 2
npm run dev
```

## 📋 ОЖИДАЕМЫЙ ВЫВОД ПОСЛЕ ЗАПУСКА:

```
🔄 Начало инициализации базы данных...
Подключено к SQLite базе данных
Таблицы созданы, база данных готова
✅ База данных инициализирована успешно
✅ База данных готова к работе
🚀 Сервер запущен на порту 3001
📡 API доступен по адресу: http://localhost:3001/api
✅ Сервер готов к работе!
```

## 🔍 ЕСЛИ ОШИБКА ВСЕ ЕЩЕ ЕСТЬ:

1. **Проверьте консоль сервера** - там должны быть детальные логи ошибки:
   ```
   🔴 === ОШИБКА РЕГИСТРАЦИИ ===
   📌 Тип ошибки: ...
   📌 Сообщение: ...
   📌 SQL запрос: ...
   ```

2. **Проверьте консоль браузера** (F12) - там также должны быть логи

3. **Убедитесь, что email полный** - например: `user@gmail.com` (не `user@gmai`)

4. **Проверьте, что все поля заполнены** правильно

## 🎯 СЛЕДУЮЩИЕ ШАГИ:

После успешной регистрации вы будете перенаправлены на:
- **Администратор** → `/admin` (страница с вкладками)
- **Староста** → `/starosta/topics` (просмотр тем)

## ⚠️ ВАЖНО:

Если ошибка сохраняется, **скопируйте из консоли сервера** все строки, начинающиеся с `🔴 === ОШИБКА РЕГИСТРАЦИИ ===` и отправьте их мне.

