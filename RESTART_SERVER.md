# Инструкция по перезапуску сервера

## Проблема: Ошибка 500 при регистрации

Если вы получаете ошибку 500 при регистрации, выполните следующие шаги:

### 1. Остановите все процессы сервера

```bash
# Найти все процессы Node.js
ps aux | grep node

# Остановить процессы сервера (замените PID на номер процесса)
kill -9 <PID>
```

Или проще:
```bash
# Остановить все процессы на порту 3001
lsof -ti:3001 | xargs kill -9
```

### 2. Убедитесь, что база данных существует

```bash
cd backend
ls -la database/database.sqlite
```

Если файл не существует, он будет создан автоматически при запуске сервера.

### 3. Перезапустите сервер

```bash
# Из корневой директории проекта
cd backend
npm run dev
```

Или из корня проекта:
```bash
npm run dev:backend
```

### 4. Проверьте логи сервера

Вы должны увидеть в консоли:
```
Начало инициализации базы данных...
Подключено к SQLite базе данных
Таблицы созданы, база данных готова
✅ База данных инициализирована успешно
🚀 Сервер запущен на порту 3001
📡 API доступен по адресу: http://localhost:3001/api
```

### 5. Проверьте работу API

Откройте браузер и перейдите по адресу:
```
http://localhost:3001/api/auth/register
```

Должна вернуться ошибка 404 или 405 (метод не разрешен), но не 500.

### 6. Если проблема сохраняется

1. Удалите базу данных и создайте заново:
```bash
cd backend/database
rm database.sqlite
cd ..
npm run dev
```

2. Проверьте права доступа к файлу базы данных:
```bash
chmod 644 backend/database/database.sqlite
```

3. Проверьте, что все зависимости установлены:
```bash
cd backend
npm install
```

## Запуск фронтенда и бэкенда одновременно

Из корневой директории проекта:
```bash
npm run dev
```

Это запустит оба сервера одновременно.

## Запуск отдельно

### Бэкенд:
```bash
cd backend
npm run dev
```

### Фронтенд:
```bash
cd frontend
npm run dev
```

## Устранение ошибок

### Ошибка: "База данных не инициализирована"
- Убедитесь, что сервер полностью запустился
- Проверьте логи сервера на наличие ошибок инициализации

### Ошибка: "Порт 3001 уже занят"
- Остановите другие процессы на этом порту
- Или измените порт в `backend/server.js` (переменная `PORT`)

### Ошибка: "Cannot find module"
- Установите зависимости: `npm install`
- Проверьте, что вы находитесь в правильной директории

