# Как запустить сервер

## 🚀 Способ 1: Запуск всего приложения (РЕКОМЕНДУЕТСЯ)

Из корневой директории проекта выполните:

```bash
npm run dev
```

Эта команда запустит:
- **Фронтенд** (Vue.js) на порту **3000**
- **Бекенд** (Express.js) на порту **3001**

## 🔧 Способ 2: Запуск по отдельности

### Запуск только бекенда:

```bash
cd backend
npm run dev
```

Бекенд будет доступен на: http://localhost:3001

### Запуск только фронтенда:

```bash
cd frontend
npm run dev
```

Фронтенд будет доступен на: http://localhost:3000

## 📋 Проверка работы серверов

### Проверить, что серверы запущены:

```bash
# Проверить порты
lsof -ti:3000,3001
```

Или откройте в браузере:
- Фронтенд: http://localhost:3000
- Бекенд API: http://localhost:3001/api

### Проверить логи:

Если серверы запущены через `npm run dev`, вы увидите логи в консоли:
- Логи фронтенда (Vite)
- Логи бекенда (Express)

## 🛑 Остановка серверов

Нажмите `Ctrl + C` в терминале, где запущены серверы.

Или убейте процессы:
```bash
# Найти процессы
lsof -ti:3000,3001

# Остановить процессы (замените PID на реальные)
kill -9 <PID>
```

## 🔄 Перезапуск серверов

1. Остановите серверы (`Ctrl + C`)
2. Запустите снова:
```bash
npm run dev
```

## ⚠️ Решение проблем

### Порт занят:

Если порт 3000 или 3001 занят, измените в:
- **Бекенд**: `backend/.env` → `PORT=3001` (измените на другой порт)
- **Фронтенд**: `frontend/vite.config.js` → `server.port: 3000` (измените на другой порт)

### Ошибка "Cannot find module":

Установите зависимости:
```bash
npm run install:all
```

### База данных не создается:

Убедитесь, что у вас есть права на запись в `backend/database/`

## 📝 Примеры команд

```bash
# Полная установка и запуск
npm run install:all
npm run dev

# Только бекенд
cd backend
npm run dev

# Только фронтенд
cd frontend
npm run dev

# Проверка портов
lsof -ti:3000,3001
```

## 🌐 Доступ к приложению

После запуска серверов:
- **Веб-интерфейс**: http://localhost:3000
- **API**: http://localhost:3001/api
