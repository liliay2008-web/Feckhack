# Отладка ошибки регистрации

## Шаг 1: Остановите все процессы сервера

```bash
cd backend
./stop-server.sh
```

Или вручную:
```bash
lsof -ti:3001 | xargs kill -9
pkill -f "node.*server.js"
```

## Шаг 2: Запустите сервер заново

```bash
cd backend
npm run dev
```

## Шаг 3: Проверьте логи сервера

После запуска вы должны увидеть в консоли:
```
Начало инициализации базы данных...
Подключено к SQLite базе данных
Таблицы созданы, база данных готова
✅ База данных инициализирована успешно
✅ База данных готова к работе
🚀 Сервер запущен на порту 3001
📡 API доступен по адресу: http://localhost:3001/api
```

## Шаг 4: Попробуйте зарегистрироваться

При попытке регистрации в консоли сервера должны появиться логи:
- `=== НАЧАЛО РЕГИСТРАЦИИ ===`
- Данные запроса
- Подготовка к сохранению пользователя
- Или `=== ОШИБКА РЕГИСТРАЦИИ ===` с деталями ошибки

## Шаг 5: Скопируйте логи ошибки

Если возникает ошибка, скопируйте из консоли сервера:
1. Все строки, начинающиеся с `=== ОШИБКА РЕГИСТРАЦИИ ===`
2. Тип ошибки
3. Сообщение ошибки
4. Код ошибки
5. SQL запрос (если есть)
6. Стек ошибки

## Возможные проблемы и решения

### Проблема: "База данных не инициализирована"
**Решение:** Убедитесь, что сервер полностью запустился. Подождите 2-3 секунды после запуска.

### Проблема: "Порт 3001 уже занят"
**Решение:** 
```bash
./stop-server.sh
# Подождите 2 секунды
npm run dev
```

### Проблема: "SQLITE_ERROR" или "SQLITE_BUSY"
**Решение:** 
1. Остановите сервер
2. Удалите файл базы данных: `rm database/database.sqlite`
3. Запустите сервер заново (база создастся автоматически)

### Проблема: Ошибка валидации email
**Решение:** Убедитесь, что email полный и корректный (например: `user@gmail.com`)

## Тестирование API напрямую

Вы можете протестировать API напрямую через curl:

```bash
curl -X POST http://localhost:3001/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "full_name": "Test User",
    "email": "test@test.com",
    "login": "testuser",
    "password": "test123",
    "role": "admin",
    "groups": null
  }'
```

Это поможет увидеть точную ошибку, возвращаемую сервером.

